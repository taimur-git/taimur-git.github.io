---
interface Props {
  body: string;
}

const { body } = Astro.props;
const mins = Math.max(1, Math.round((body.trim().length + 240) / 1440));
---

<span class="mono reading-time">{mins} min read</span>
